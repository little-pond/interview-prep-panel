# === Layer-1 transcript corpus — NOT committed ===
# Large (~940MB) and contains creators' copyrighted YouTube captions.
# Regenerate locally with: bash build/pull_transcripts.sh   (or update_knowledge.sh)
build/subs/
build/transcripts/
build/samples/
build/*_corpus.txt
build/*_subs.log
build/*_err.log

# NotebookLM pipeline artifacts (unused path — CLI auth blocked by Google)
build/*_notebook_id.txt
build/*_added.txt
build/*_retrieval.md

# macOS / editor cruft
.DS_Store
**/.DS_Store
*.swp
